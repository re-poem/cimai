# ClangSharpPInvokeGenerator response file.
# 运行方式（在 bindings/CSharp 目录下）：
#   dotnet tool run ClangSharpPInvokeGenerator @generate.rsp
#
# 路径相对于 bindings/CSharp（MSBuild 目标会以该项目目录为工作目录执行）。
# -f  解析入口（umbrella 头）
# -I  include 根，使 <cimai/...> 与 <thirdparty/...> 都能被解析
# -t  需要为其生成绑定而被遍历的 API 头（ClangSharp 默认不遍历 #include 到的头）
-f
../../include/cimai/cimai.h
-I
../../include
-t
../../include/cimai/parser.h
-t
../../include/cimai/chart_parser.h
-t
../../include/cimai/meta_parser.h
-t
../../include/cimai/simai_types.h
-n
Cimai.Native
-o
Native.g.cs
-l
cimai
-x
c
